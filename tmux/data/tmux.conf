# LDVSoft Environment :: Tmux
# Target version: 3.1

# This is a driver for loading configuration.

if-shell "@@EXECDIR_PRIVATE@@/tmux-render-config" {
	source-file "$XDG_RUNTIME_DIR/@@PRIVATE_NAME@@/tmux.conf-rendered"
} {
	display-message 'Failed to load configuration.'
}
